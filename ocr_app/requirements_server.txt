# Document extraction server — lightweight, NO GPU needed
# The server itself is just a FastAPI app that calls LLM/VLM endpoints.
# GPU work happens in vLLM / Ollama (separate process).
#
# Install: pip install -r ocr_app/requirements_server.txt

# --- Server ---
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9
httpx>=0.27.0
pydantic>=2.0

# --- PDF text extraction ---
pymupdf>=1.24.0

# --- Image handling (for VLM fallback on scans) ---
Pillow>=10.0.0
